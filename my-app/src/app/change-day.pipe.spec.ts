import { ChangeDayPipe } from './change-day.pipe';

describe('ChangeDayPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDayPipe();
    expect(pipe).toBeTruthy();
  });
});
